type IntegrationStep {
  step: String!
}

input CreateIntegrationInput {
  id: String!
}

# type Query {}

type Mutation {
  createIntegration(createIntegrationInput: CreateIntegrationInput!): Boolean!
}

type Subscription {
  integrationCreated(id: String!): [IntegrationStep]
}
